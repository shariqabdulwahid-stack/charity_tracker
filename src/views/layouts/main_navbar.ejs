<nav class="navbar navbar-expand-lg bg-primary">
  <div class="container-fluid">
    <!-- Brand on the left -->
    <a class="navbar-brand fw-bold text-dark" href="/">
      <i class="bi bi-heart-fill me-1 text-danger"></i> Charity Tracker
    </a>

    <!-- Toggler for mobile -->
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
      <span class="navbar-toggler-icon"></span>
    </button>

    <!-- Links on the right -->
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav ms-auto">
        <!-- Campaigns -->
        <li class="nav-item">
          <a class="nav-link text-dark fw-semibold" href="/campaigns">
            <i class="bi bi-flag me-1"></i> Campaigns
          </a>
        </li>

        <!-- Login / Logout -->
        <% if (!req.session?.user) { %>
          <li class="nav-item">
            <a class="nav-link text-dark fw-semibold" href="/auth/login">
              <i class="bi bi-box-arrow-in-right me-1"></i> Login
            </a>
          </li>
        <% } else { %>
          <li class="nav-item">
            <a class="nav-link text-dark fw-semibold" href="/auth/logout">
              <i class="bi bi-box-arrow-right me-1"></i> Logout
            </a>
          </li>
        <% } %>
      </ul>
    </div>
  </div>
</nav>
